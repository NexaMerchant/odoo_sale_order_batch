<odoo>
    <data>
        <template id="custom_sale_report_template" name="Custom Sale Report Template">
            <t t-call="web.html_container">
                <div class="my_template" style="text-align: center;">
                    <h1>欢迎使用 Odoo 16 模板视图</h1>
                    <p>这是一个自定义模板的示例。</p>
                    <t t-foreach="docs" t-as="doc">
                        <div>
                            <p>订单编号：<t t-esc="doc.name"/></p>
                            <p>总计：<t t-esc="doc.amount_total"/></p>
                        </div>
                    </t>
                </div>
            </t>
        </template>
    </data>
</odoo>